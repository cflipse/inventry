<ul class='px-3 py-2'>
  <% category_counts.each do |category, count| %>
    <li>
      <%= category %>
      <span class='ml-1 mr-2 font-semibold text-xs text-teal-700 bg-teal-200 rounded-full px-1 py-1 inline-block align-baseline'>
        <%= count %>
      </span>
    </li>
  <% end %>
</ul>
